; ============================================================
; CulturesGameExtend - Global 配置（默认/共享）
; 由 CulturesGameExtend_Game.ini 覆盖同名键（Global 打底、Game 覆盖）
; ============================================================

[General]
; 日志级别: Trace / Debug / Info / Warn / Error / None
LogLevel = Info

[Version]
; 主程序 exe 名称（只有这一个程序）
ExeName = Game.exe
