# 修复：攻击半径硬编码上限 18 -> 30，避免无知军事模式下无限循环
# 格式: <addr> <hex bytes>   (addr 相对 image base 0x400000)
# Fix: hard-coded attack-radius cap 18 -> 30, avoids infinite loop in ignorant-military mode
# Format: <addr> <hex bytes>   (addr relative to image base 0x400000)
# fixAttackRadiusFreeze.ini
# CMP EAX, 0x1E
0x6a156 83 F8 1E | 83 F8 12
