# 修复：地图边缘分配 marker 时崩溃（将 JNZ 改为两字节 NOP）
# 格式: <addr> <hex bytes>   (addr 相对 image base 0x400000)
# Fix: crash when assigning a marker at the map edge (change JNZ to a two-byte NOP)
# Format: <addr> <hex bytes>   (addr relative to image base 0x400000)
# fixMarkerOnBorderCrash.ini
0xbaab 66 90 | 75 27
